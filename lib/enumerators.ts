export enum LoginInfo {
    OK = "You have logged in as ",
    ERR = "Incorrect username or password"
}

export enum RegistrationInfo {
    OK = "You have registered as ",
    ERR = "Username or email already exists"
}

export enum HTTPCodes {
    OK = 200,
    ERR = 400
}